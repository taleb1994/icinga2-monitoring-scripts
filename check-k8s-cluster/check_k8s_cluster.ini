object CheckCommand "check_k8s_cluster" {
    command = [ "sudo", "-u", "root", "/etc/icinga2/scripts/check_k8s_cluster.sh" ]
    // This check does not require arguments as it inspects the entire cluster
    // Description
    vars.check_description = "Health check of the Kubernetes cluster"
}