zones.d/master/hosts.conf
object Host "icinga.monitoring.heuboe.hbintern" {
    display_name = "icinga-master"
    address = "192.168.195.100"
    check_command = "hostalive"
    max_check_attempts = "3"
    check_interval = 30s
    retry_interval = 10s
    check_timeout = 8s
    enable_notifications = true
    enable_active_checks = true
    enable_passive_checks = true
    enable_event_handler = true
    enable_perfdata = true
    command_endpoint = "icinga.monitoring.heuboe.hbintern"
    groups = [ "@all" ]
}
