template Notification "service-notification-template" {
    command = "mail-service-notification"
    interval = 12h
    period = "24x7-timeperiod"
    states = [ Critical, Unknown, Warning ]
    types = [
        Acknowledgement,
        Custom,
        DowntimeEnd,
        DowntimeRemoved,
        DowntimeStart,
        FlappingEnd,
        FlappingStart,
        Problem,
        Recovery
    ]
    users = [ "mohammed-taleb" ]
}
